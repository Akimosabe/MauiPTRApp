<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MauiPTRApp.ViewModels"
             x:Class="MauiPTRApp.MainPage">

    <ContentPage.BindingContext>
        <vm:MainViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout Padding="10">
            <!-- Заголовок -->
            <Label Text="Список книг" FontSize="24" HorizontalOptions="Center" Margin="0,0,0,20" />

            <!-- Список книг -->
            <CollectionView ItemsSource="{Binding Books}" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5" ColumnDefinitions="*,Auto,Auto" RowDefinitions="Auto,Auto">
                            <!-- Название книги -->
                            <Label Text="{Binding Title}" FontSize="18" VerticalOptions="Center" />

                            <!-- Автор -->
                            <Label Text="{Binding Author}" FontSize="14" TextColor="Gray" Grid.Row="1" VerticalOptions="Center" />

                            <!-- Кнопка редактирования -->
                            <Button Text="Редактировать" FontSize="16" 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainViewModel}}, Path=EditCommand}"
                                    CommandParameter="{Binding}" HorizontalOptions="End" />

                            <!-- Кнопка удаления -->
                            <Button Text="Удалить" FontSize="16" 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MainViewModel}}, Path=DeleteCommand}"
                                    CommandParameter="{Binding}" HorizontalOptions="End" Grid.Row="1" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Кнопка добавления -->
            <Button Text="Добавить книгу" FontSize="18" Command="{Binding AddCommand}" HorizontalOptions="Center" Margin="0,20,0,0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
